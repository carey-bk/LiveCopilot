# ── Secrets (NEVER commit) ─────────────────────────────
# Local code-signing identity: private keys + PKCS#12 bundle.
# Never export signing credentials into the repository.
StealthApp/.signing/
*.p12
*.pem
*.key
*.p8
*.mobileprovision
*.provisionprofile

# ── Xcode / build artifacts ────────────────────────────
build/
DerivedData/
*.xcodeproj          # generated from project.yml by xcodegen
xcuserdata/
*.xcworkspace/xcuserdata/

# ── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon?

# ── Editors ────────────────────────────────────────────
.idea/
.vscode/
*.swp

# LiveCopilot local data, private documents and secrets
.env
.env.*
!.env.example
*.sqlite
*.sqlite-*
*.db
*.xcresult/
private/
local-data/
.signing/
.build/
dist/
_site/
StealthApp/Resources/LocalRuntime/
*.dmg

# Preserve the supplied reference document locally, outside source delivery.
docs/LiveCopilot_Codex_Goal_V1.docx
